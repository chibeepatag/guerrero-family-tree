<!DOCTYPE html>
<html>
  <head>
    <title>GuerreroFamilyTree</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
  </head>

  <body>
    <%= yield %>
    <%if @footer%>
    <footer>
      <div class="row">
        <div class="col-auto me-auto"><%=@member.branch%></div>
        <div class="col-auto"><%=@member.name%></div>
      </div>
    </footer>
    <%end%>
  </body>
</html>
